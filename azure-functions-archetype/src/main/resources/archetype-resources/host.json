{
  "version": "2.0",
#if(${trigger.toLowerCase()}=="durablefunctions")
  "extensions": {
    "durableTask": {
      "hubName": "JavaTestHub"
    }
  },
#end
#set($openaiBindings = ["assistantskilltrigger", "assistantcreate", "assistantquery", "assistantpost", "embeddingsinput", "embeddingsstoreoutput", "semanticsearch", "textcompletion"])
#if(${trigger.toLowerCase().startsWith("dapr")} || ${trigger.toLowerCase()}=="sqltrigger" || ${trigger.toLowerCase()}=="sql" || $openaiBindings.contains(${trigger.toLowerCase()}))
  "extensionBundle": {
    "id": "Microsoft.Azure.Functions.ExtensionBundle.Preview",
    "version": "[4.*, 5.0.0)"
  }
#else
  "extensionBundle": {
    "id": "Microsoft.Azure.Functions.ExtensionBundle",
    "version": "[4.*, 5.0.0)"
  }
#end
}
